# sets
import math

myset = {"apple", "banana", "cherry"}
myset.add("lemon")

# lambda function

pow = lambda x: x*x

add = lambda x, y: x+y

convert = lambda c: str(c)

g = lambda x: math.pow(x,2)/math.sqrt(5) #g(x) = x^2/sqrt(5)
